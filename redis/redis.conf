# Minimal Redis configuration for development
# Passing a config file avoids the startup notice and allows cross-container access.

port 6379
bind 0.0.0.0
protected-mode no

# Disable snapshots to avoid background fork in constrained dev envs
save ""

dir /data
appendonly no

# Connection stability
tcp-keepalive 60
timeout 0
